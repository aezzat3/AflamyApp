import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  card: {
    alignItems: 'center',
    justifyContent: 'flex-start',
    width: 100,
  },
  avatar: {
    width: 60,
    height: 60,
    borderRadius: 30,
    marginBottom: 5,
  },
  name: {
    textAlign: 'center',
    fontSize: 12,
  },
});
